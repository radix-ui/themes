import { Section, Image, Flex, Text, Separator, Grid, Card, Heading } from '@kushagradhawan/kookie-ui';
import Link from 'next/link';
import { Download, Grid3X3, Palette, Settings, GitBranch, Dog, Power } from 'lucide-react';

<Section size="1">

<Power />
# Start

To get started, install the library and import the stylesheet. Wrap your app with `Theme` to set `accentColor`, `grayColor`, and optional `material`. Compose layouts using Shell/Sidebar patterns and build UIs with components at matching sizes. Refer to Foundations for tokens to guide your scale choices. See Installation for framework‑specific steps.

</Section>

<Grid columns={{ initial: '1', sm: '2' }} gap="2" my="6">
  {[
    {
      href: '/docs/installation',
      title: 'Installation',
      description: 'Get Kookie UI set up in your project with any React framework',
      icon: Download,
      image: '/install.png',
    },
    {
      href: '/docs/button',
      title: 'Components',
      description: 'Browse and explore all available components and patterns',
      icon: Grid3X3,
      image: '/box.png',
    },
    {
      href: '/docs/theme',
      title: 'Foundations',
      description: 'Design tokens, colors, typography, and spacing systems',
      icon: Palette,
      image: '/foundation.png',
    },
    
  ].map(({ href, title, description, icon: Icon, image }) => (
    <Card asChild key={href} size="2" variant="soft">
      <Link href={href}>
        <Flex direction="row" align="center" gap="4">
        
          {/*
          <Flex align="center" justify="center" style={{ width: 96, height: 96, flex: '0 0 96px' }}>
            <Image src={image} alt="" width={96} height={96} fit="cover" />
          </Flex>
          */}
           
          <Flex direction="column" gap="2" p="2">
            <Flex align="center" gap="2">
              <Icon size={16} />
              <Heading size="3" weight="medium">
                {title}
              </Heading>
            </Flex>
            <Text size="2" color="gray">
              {description}
            </Text>
          </Flex>
        </Flex>
      </Link>
    </Card>
  ))}
</Grid>
