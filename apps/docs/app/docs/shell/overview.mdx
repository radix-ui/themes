import { Card, Flex, Text, Heading, Grid, Badge, Box, Section, Separator } from '@kushagradhawan/kookie-ui';
import Link from 'next/link';
import { Play, Code, Users, BookOpen, Ruler, History } from 'lucide-react';

<Section size="1">

<Badge size="2" variant="soft" highContrast color="orange" style={{ alignSelf: 'flex-start' }}>Beta</Badge>

# Shell

The Shell component is a complete layout engine that provides structural patterns for building application interfaces. It manages layout state, composition rules, and responsive behavior across seven core slots: Header, Rail, Panel, Sidebar, Content, Inspector, and Bottom.

<Grid columns={{ initial: '1', sm: '2' }} gap="4" mt="9">
  {[
    {
      href: '?tab=playground',
      title: 'Playground',
      description: 'Interactive layout explorer with live shell configurations and examples',
      icon: Play,
    },
    {
      href: '?tab=api',
      title: 'API',
      description: 'Complete component reference with props, hooks, and integration patterns',
      icon: Code,
    },
    {
      href: '?tab=accessibility',
      title: 'Accessibility',
      description: 'Keyboard navigation, screen readers, and inclusive layout patterns',
      icon: Users,
    },
    {
      href: '?tab=guidelines',
      title: 'Guidelines',
      description: 'Layout patterns, composition rules, and architectural best practices',
      icon: BookOpen,
    },
    {
      href: '?tab=specs',
      title: 'Specs',
      description: 'Design tokens, measurements, and responsive breakpoint specifications',
      icon: Ruler,
    },
    {
      href: '?tab=changelog',
      title: 'Changelog',
      description: 'Release notes, layout updates, and version history',
      icon: History,
    }
  ].map(({ href, title, description, icon: Icon }) => (
    <Card asChild key={href} size="2" variant="soft">
      <Link href={href}>
        <Flex direction="column" gap="2" p="2">
          <Flex align="center" gap="2">
            <Icon size={16} />
            <Heading size="2" weight="medium">{title}</Heading>
          </Flex>
          <Text size="2" color="gray">{description}</Text>
        </Flex>
      </Link>
    </Card>
  ))}
</Grid>

</Section>