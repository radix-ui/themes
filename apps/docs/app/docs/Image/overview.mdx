import { Card, Flex, Text, Heading, Grid, Badge, Image } from '@kushagradhawan/kookie-ui';
import Link from 'next/link';

# Image

{/* <Image style ={{marginBottom: '100px'}} shadow='2' radius='full' variant='blur' src="/Button.png" alt="Image component overview" /> */}

Display images with proper loading states, accessibility support, and responsive behavior. The Image component handles common image display patterns including lazy loading, skeleton placeholders, and graceful error handling.

Choose from different object-fit options to control how images scale within their containers, and use the asChild pattern to make images interactive. The component automatically provides accessibility compliance, loading state management, and responsive behavior out of the box.

For optimal performance, the component supports lazy loading by default and provides skeleton placeholders to improve perceived loading times.

## Guide

<Grid columns={{ initial: '1', sm: '2' }} gap="4" my="6">
  {[
    {
      href: '?tab=api',
      title: 'API',
      description: 'Props, integration patterns, and technical implementation for developers',
    },
    {
      href: '?tab=accessibility',
      title: 'Accessibility',
      description: 'Screen reader support, loading announcements, and inclusive design patterns',
    },
    {
      href: '?tab=guidelines',
      title: 'Guidelines',
      description: 'Performance, user experience, and design principles for effective image display',
    },
    {
      href: '?tab=specs',
      title: 'Specs',
      description: 'Design tokens, measurements, and system specifications',
    }
  ].map(({ href, title, description, badge }) => (
    <Card asChild key={href} size="3" variant="classic">
      <Link href={href}>
        <Flex direction="column" gap="2">
          <Heading size="3" weight="medium">{title}</Heading>
          <Text size="3" color="gray">{description}</Text>
        </Flex>
      </Link>
    </Card>
  ))}
</Grid> 